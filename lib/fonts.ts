// Use system fonts to avoid network timeouts during build
export const fontSans = {
  variable: "--font-sans",
  className: "",
}

export const fontMono = {
  variable: "--font-mono", 
  className: "",
}
